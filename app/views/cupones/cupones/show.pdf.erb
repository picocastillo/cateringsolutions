<% content_for :styles do %>
  @page {
    size: 50mm 30mm;
    margin: 0;
  }
  body {
    font-family: Calibri;
    text-align: center;
  }
<% end %>

<%= render partial: 'cupon_ticket', locals: { cupon: @cupon } %>
